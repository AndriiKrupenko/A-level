// Сделайте цикл с prompt, который прерывается по нажатию OK и продолжается по нажатию "Отмена" c пустым телом цикла.
//------------------


for (let loop = prompt(); loop === null ; loop=prompt()) {
}
